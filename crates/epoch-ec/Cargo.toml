[package]
name = "epoch-ec"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

# NOTE: epoch-ec is pure computation (L1). It must NOT depend on tokio or any
# I/O crate; I/O is injected via traits. Enforced by `cargo xtask layers`.
[dependencies]
thiserror = { workspace = true }
reed-solomon-simd = { workspace = true }
blake3 = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }
proptest = { workspace = true }

[[bench]]
name = "ec"
harness = false
